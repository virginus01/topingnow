version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next/server
    files:
      - "**/*"
  cache:
    - node_modules/**/*
